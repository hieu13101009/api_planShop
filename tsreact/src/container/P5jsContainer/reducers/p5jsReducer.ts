interface HomeState {

}

const defaultState: HomeState = {};

export function p5jsReducer(state: HomeState = defaultState, action: any): HomeState  {
    switch (action) {
        default:
            return state
    }
}